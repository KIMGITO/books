export interface Teachers {
    id: number;
    first_name: string;
    last_name: string;
}

export interface Levels {
    id: number;
    name: string;
}


export interface Active {
    active: string;
}